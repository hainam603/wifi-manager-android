package com.antigravity.wifimanager.ui.theme

import androidx.compose.ui.graphics.Color

// Bảng màu Dark Mode Kính mờ (Glassmorphism Dark)
val Slate900 = Color(0xFF0F172A)
val Slate950 = Color(0xFF020617)
val CardBackground = Color(0x11FFFFFF) // Nền kính mờ siêu mỏng
val CardBorder = Color(0x22FFFFFF)     // Viền kính mờ phản chiếu

// Màu sắc đại diện chất lượng WiFi (HSL Dynamic)
val WifiGood = Color(0xFF10B981)      // Xanh lục neon (Sóng khỏe)
val WifiMedium = Color(0xFFF59E0B)    // Vàng hổ phách (Sóng vừa)
val WifiWeak = Color(0xFFEF4444)      // Đỏ ruby (Sóng yếu)
val WifiDisconnected = Color(0xFF64748B) // Xám Slate (Không kết nối)

// Màu sắc chủ đạo giao diện
val Primary = Color(0xFF6366F1)       // Tím Indigo hiện đại
val PrimaryVariant = Color(0xFF4F46E5)
val Accent = Color(0xFF06B6D4)        // Xanh Cyan năng động
val TextPrimary = Color(0xFFF8FAFC)   // Chữ trắng sáng
val TextSecondary = Color(0xFF94A3B8) // Chữ xám mờ
